package com.ridoh.Order_Management.enums;

/**
 * Enum representing the different roles a user can have in the order management system.
 */
public enum UserRole {

    /** Represents an administrator with full access to manage users, orders, and products. */
    ADMIN,

    /** Represents a regular user who can place orders and manage their account. */
    USER
}
